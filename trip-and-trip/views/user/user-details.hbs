<div class="container">

    <h1>{{userData.username}}</h1>

    <hr>
    <img class="card-img-top" src="{{userData.avatar.[0]}}" alt="Card image cap">
    <hr>
    {{#each commentData}}
    <div class="card border-dark drone-card">
        <div class="card-body">
            <p class="card-title"><strong>{{this.commenter.username}}</strong></p>
            <p class="card-text">{{this.content}}</p>
        </div>
    </div>
    {{/each}}
    {{!-- <form action="/users/{{id}}" method="POST"></form>
    <div class="mb-3">
        <label for="comments" class="form-label">Comments:</label>
        <input type="text" class="form-control" id="comment" name="comment">
    </div>
    <button type="submit" class="btn btn-dark">Comment</button>
    </form> --}}

    <form action="/users/{{id}}/comment" method="post">
        <textarea class='form-control' name="content" placeholder="Comment"></textarea>
        <div class="text-right">
            <button type="submit" class="btn btn-primary">Save</button>
        </div>
    </form>

</div>